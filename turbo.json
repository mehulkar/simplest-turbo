{
    "$schema": "https://turborepo.org/schema.json",
    "pipeline": {
        "//#build": {
          "inputs": [".envxx"]
        },
        "build": {
            "dependsOn": ["^build"],
            "inputs": [
                ".env"
            ]
        }
    }
}

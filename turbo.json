{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": [".env", "$NODE_ENV"],
  "pipeline": {
    "build": {
      "env": ["A"]
    },
    "lint": {
      "env": ["B"]
    }
  }
}
